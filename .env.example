GROQ_API_KEY=your_groq_api_key_here
# Optional: a GitHub token raises API rate limits (60→5000/hr) and allows private repos.
# Not required to review public PRs. Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
